# kuaimai-cli 每阶段新增能力

本文档记录各开发阶段完成后系统**新增**的能力，便于版本对照与验收。  
**当前代码版本**：`meta_data.json` **v1.6.0**（`item` **1157** operations），业务 shortcuts 对接 **erp-items-core**（`https://erp1.superboss.cc/`），路径**不**追加 `.json` 后缀。元数据字段与命名规则见 [meta_data.json 定义规范](./kuaimai-cli%20meta_data.json%20定义规范.md)。

**框架状态（2026-06-02）**：meta 契约 + Skill 规则 + CLI 基础能力（含 `--page-limit` / `--page-confirm` 分页防护）**已闭环**；后续新增接口 primarily 登记 meta + 可选 shortcut/Skill。

---

## 当前业务里程碑：商品标题能力（已基本可用）

通过 CLI 可完成与页面一致的**商品标题**相关操作：

| 步骤 | 命令 | 说明 |
|------|------|------|
| 1. 按标题查商品 | `item +list` / `item list` | `--body` 中 `title` 模糊/精确筛选（form → `/item/stock/queryList`） |
| 2. 取系统 ID | 列表 `data` 中 `sysItemId` | 可与 `count` 核对总数 |
| 3. 拉详情（可选） | `item get-detail --sys-item-id <id>` | GET `/item/getItemDetail` |
| 4. 改标题保存 | `item update-title`（推荐）或 `item save` | `update-title` 内部 get-detail 合并后 POST `/item/saveItem`；支持 `--dry-run` |

鉴权：`auth login` 后请求头自动携带 `accessToken`（见仓库根 [README.md](../README.md)）。

> **历史说明**：早期迭代曾对接 erp-scm（scm3、9 业务域、`.json` 后缀）。当前仓库已收敛为 **erp-items-core 商品域**；meta v1.6.0 覆盖 `/item` Controller 全量接口。

---

## 阶段一：基建骨架版（已完成）

**定位**：最小生产可用（MVP），约 **60%** 飞书体验。  
**目标**：配置、鉴权、原始 API、结构化输出、业务 shortcuts 形态（飞书 `+` 语法）。

### 1. 架构与工程

| 能力 | 说明 |
|------|------|
| 分层目录 | `cmd/` · `internal/` · `shortcuts/` · `pkg/` · `skills/` |
| 单二进制 | `make build` / `go build -o kuaimai-cli` |
| 分层铁规 | 业务仅在 `shortcuts/`；`cmd` 无网络/业务逻辑 |

### 2. 全局能力

`--help` · `--verbose` · `--output table|json` · `--dry-run`

### 3. 配置管理

| 命令 | 说明 |
|------|------|
| `config init` | 创建 `~/.kuaimai-cli/config.yaml` |
| `config get [key]` | 查看配置 |
| `config set <key> <value>` | 修改并落盘 |

**调用链**：`config init` → `internal/config.Init()` → `template.go` → `~/.kuaimai-cli/config.yaml`（0700/0600）

**输出优先级**：`--output` > `cli.output` > `table`

**默认 API**：`https://erp1.superboss.cc/`

### 4. 鉴权

| 命令 | 说明 |
|------|------|
| `auth login <accessToken>` | 写入系统密钥链（**唯一方式**）；可选 `--profile`、`--api-url` |
| `auth status` / `auth logout` / `auth check` | 状态 / 清除 / 探测 token 与 API |
| `auth list` / `auth use <profile>` | 多账号 profile 列表与切换 |

请求头：`accessToken: <token>`（非 Bearer；由 `auth login` 写入密钥链后自动附加）

### 5. 原始 API

```bash
kuaimai-cli api GET|POST|PUT|DELETE <path> [--body '{}']
```

### 6. 输出规范

stdout：`{ok, data, error, hint}` · stderr：日志 · 无堆栈

### 7. 阶段一尚未具备（由阶段二补齐）

`schema`/`service`、彩色输出、completion、重试、`--page-all`、form 列表等 → 见阶段二。

---

## 阶段二：标准企业版（已完成）

**定位**：正式企业版基建，约 **80%** 飞书体验（执行管线 + 元数据命令 + 终端体验）。

### 1. 统一执行管线（shortcuts/common）

| 组件 | 说明 |
|------|------|
| `runner` | `ExecuteList` / `ExecuteWrite`、鉴权、dry-run |
| `BuildPath` / `SetQuery` | GET 查询参数拼接 |
| `RunGET` / `RunPOST` / `RunPOSTForm` / `RunPOSTFormListMap` | 各 HTTP 形态封装 |

### 2. 元数据驱动（registry + service + schema）

| 组件 | 路径 | 说明 |
|------|------|------|
| 注册表 | `internal/registry/meta_data.json` | 当前 **v1.6.0**，1 service / **1157** operations |
| 命令 | `kuaimai-cli service item <operation>` | 如 `service item stock-list --body '{"pageNo":1,"pageSize":50}'` |
| 自省 | `kuaimai-cli schema` | 从 meta 读取并输出全部 operation（含 requestSchema/responseSchema、shortcut 映射） |
| 拉取/生成 | `scripts/generate_meta/` · `scripts/filter_meta/` · `scripts/normalize_meta/` | 从 erp-items-core 生成并过滤 meta |
| 拉取（可选） | `scripts/fetch_meta/fetch_meta.sh` | 可选 `KUAIMAI_OPENAPI_URL` |

### 3. 鉴权（accessToken）

`auth login <accessToken>` → 密钥链 → 请求头 `accessToken`

### 4. 终端体验

彩色 table · `cli.color` · `--no-color` · `completion bash|zsh|powershell`

### 5. 网络层

`api.retry`（默认 3）· 指数退避 · `--page-all`（`pageNo`/`pageSize`）· **`--page-limit`** · **`--page-confirm`**

### 6. dry-run

`service` 写操作 · `api POST|PUT|DELETE` · `item save` 等写 shortcuts

### 7. 阶段二验收命令速查

```bash
kuaimai-cli config init
kuaimai-cli auth login "<accessToken>"
kuaimai-cli schema --output json
kuaimai-cli service item stock-list --help
kuaimai-cli api POST /item/stock/queryList --body '{}' --verbose
```

### 8. 阶段二尚未具备

erp-items-core 业务 shortcuts 闭环 · Skill · csv/ndjson · 连接池/熔断 · 脱敏 · 审计 → 见阶段三。

---

## 阶段三：平台进阶版（已完成，erp-items-core 商品域）

**定位**：平台进阶 ~95%，围绕 **商品标题查改** 与 Agent 协作补齐能力。

### 1. erp-items-core 商品 shortcuts

| 子命令 | Method | 路径 | 说明 |
|--------|--------|------|------|
| `list` / `+list` | POST form | `/item/stock/queryList` | 库存列表；`title` 等筛选与浏览器 form 一致 |
| `count` | POST form | `/item/stock/queryCount` | 与 list 相同筛选条件下的总数 |
| `get-detail` | GET | `/item/getItemDetail` | `--sys-item-id` |
| `save` | POST JSON | `/item/saveItem` | 新建/修改；复杂字段改写字段 |
| `update-title` | GET+POST | `getItemDetail` + `saveItem` | **改标题**推荐；`--sys-item-id` + `--title` |

**带 `+` 的快捷命令**：`item +list`。

**常用 flags**：

- `list`/`count`：`--body` JSON（转为 `application/x-www-form-urlencoded`）
- `get-detail`：`--sys-item-id`；可选 `--api-name`
- `save`：`--body`（SysItemModel JSON）；`--sync-hs-code`；`--dry-run`
- 全局：`--page-all`、`--output csv|ndjson`

**列表默认值**（与 ARCHIVE_V2 商品库存页对齐）：`pageType=ITEM_STOCK`、`subPageType=ARCHIVE_V2`、`api_name=item_stock_queryList` 等，见 `shortcuts/item/item.go`。

### 2. Skill 平台

| 命令 | 说明 |
|------|------|
| `skill list` | 扫描 `~/.agents/skills` 等 Agent 目录 |
| `skill install` | 从 GitHub 安装默认 Skill（`kuaimai-shared` + `kuaimai-item`），**覆盖写入**各 Agent 目录 |
| `skill install --if-stale` | 仅在 Release/CLI 变化或未安装时更新默认 Skills |
| `skill install <name>` | 安装单个 Skill **整目录**到 5 个 Agent 目录 |

**内置 Skill**（仓库 `skills/`）：

| Skill | 说明 |
|-------|------|
| `kuaimai-shared` | 全局约定：配置、鉴权、输出信封、安装与排错 |
| `kuaimai-item` | 商品域路由 + **8** 个 `references/`（含 meta-execution、service、query-list-v2） |

Skill 风格对标飞书 lark-*：域 Skill 顶部 **CRITICAL** 读 shared；Shortcuts 表链 `references/`；写操作须先 Read 对应 reference。

### 3. 多格式输出

| `--output` | 行为 |
|------------|------|
| `csv` | 列表成功时输出 CSV |
| `ndjson` | 列表成功时每行一条 JSON |
| `json` / `table` | 保持 `{ok,data,error,hint}` 信封 |

### 4. 企业级网络

| 配置项 | 说明 |
|--------|------|
| `api.pool_max_idle` | 连接池最大空闲连接（默认 100） |
| `api.pool_max_idle_per_host` | 每主机空闲连接（默认 10） |
| `api.circuit_threshold` | 连续失败触发熔断阈值（默认 5） |
| `api.circuit_cooldown_sec` | 熔断冷却秒数（默认 30） |

### 5. 脱敏与安全

- `pkg/sanitize`：日志 / dry-run 中 `accessToken`、`password` 等脱敏
- Token 仅通过 `auth login`，请求自动带 `accessToken` 头

### 6. item 增强

| 能力 | 说明 |
|------|------|
| `--page-all` | `pageable:true` 接口自动翻页（form `pageNo`/`pageSize` 或 GET `page`/`page_size`） |
| `--page-limit` | 最大拉取条数 |
| `--page-confirm` | 500/1000 阈值交互续查（`prompt`/`yes`/`no`） |
| 分片合并 | `internal/pagination` 每 500 条一块，降低内存峰值 |
| 审计日志 | `~/.kuaimai-cli/audit.log` 记录业务命令 |

### 7. Agent 约定

根目录 `AGENTS.md` 已约定 item 域优先 shortcuts、输出格式与安全（阶段四扩展 E2E / 升级）。

### 8. 阶段三验收命令速查（标题能力）

```bash
kuaimai-cli auth login "<accessToken>"

# 按标题查
kuaimai-cli item +list \
  --body '{"title":"关键字","pageNo":1,"pageSize":20}' \
  --output json

# 改标题（推荐 update-title，先试跑加 --dry-run）
kuaimai-cli item update-title \
  --sys-item-id <id> \
  --title "新标题" \
  --dry-run --verbose

kuaimai-cli doctor --output json
kuaimai-cli skill list
kuaimai-cli skill install
```

---

## 阶段四：分发与 Agent 生态（部分完成）

| 能力 | 状态 | 说明 |
|------|------|------|
| GitHub Release | 已具备 | `.goreleaser.yaml` + `.github/workflows/release.yml`，6 平台二进制 + `checksums.txt` |
| npm 分发 | 已具备 | `npm/` 目录，`npx @kuaimai-cli/cli install` |
| Agent 安装指南 | 已具备 | [快麦 CLI 安装（Agent 专用）.md](./快麦 CLI 安装（Agent 专用）.md) |
| Skill 远程安装 | 已具备 | `skill install`（GitHub Contents API 整目录，5 个 Agent 目录） |
| Skill 飞书风格 | 已具备 | CRITICAL 路由、`references/` 工作流、`doctor` 检测 references |
| 版本号 | 已具备 | `kuaimai-cli --version` · `internal/build` ldflags |

| E2E 冒烟 | 已具备 | `tests/cli_e2e` + CI workflow |
| `upgrade` | 已具备 | 默认 npm 一键升级 + Skills 同步；`--check-only` 仅检查；命令后 stderr 提示（24h 缓存） |
| `skill install --if-stale` | 已具备 | Release/CLI 变化或未安装时才覆盖更新 |
| `doctor` | 已具备 | 配置/鉴权/PATH/Skill 自检 |
| 多账号 profile | 已具备 | `auth login --profile`、`auth list`、`auth use` |
| `auth check` | 已具备 | 探测 token + API |
| `item update-title` | 已具备 | get-detail 合并后 save |
| README / CHANGELOG / Docker | 已具备 | 根目录文档与 Dockerfile |

**仍规划中**：更多 curated shortcuts · `--page-delay` · 单 op `schema`

---

## 框架闭环里程碑（2026-06-02）

| 能力层 | 状态 |
|--------|------|
| meta 契约（v1.6.0 / 1157 op） | ✅ |
| Skill 规则（v2.0.0 + references） | ✅ |
| CLI contentType / dry-run / Schema | ✅ |
| CLI 海量数据分页防护 | ✅ |
| CLI 升级与 Skill 同步 | ✅ `upgrade` 默认一键升级；stderr 新版本提示；`--if-stale` |
| 后续新增接口 | 登记 meta →（可选）shortcut/Skill → 验收 |

---

## 附录：shortcuts 与 meta operation 对照表（当前）

| shortcuts | meta operation | Method | Path | contentType | write | pageable |
|-----------|----------------|--------|------|-------------|-------|----------|
| `item list` / `+list` | `stock-list` | POST | `/item/stock/queryList` | post_form | false | true |
| `item count` | `stock-count` | POST | `/item/stock/queryCount` | post_form | false | false |
| （无 shortcut） | `item-query-list-v2` | POST | `/item/queryListV2` | post_form | false | true |
| `item get-detail` | `item-detail` | GET | `/item/getItemDetail` | get_query | false | false |
| `item save` | `item-save` | POST | `/item/saveItem` | post_json | true | false |
| `item update-title`（get-detail→save 编排） | `item-update-title`（原子 saveItem） | POST | `/item/saveItem` | post_json | true | false |

> Agent **优先 shortcuts** 左列；`service item <operation>` 走 meta 原子 API。`update-title` 的多步编排在 shortcuts 实现。

源码：`shortcuts/item/` · 元数据：`internal/registry/meta_data.json` **v1.6.0** · 规范：[meta_data.json 定义规范](./kuaimai-cli%20meta_data.json%20定义规范.md)
